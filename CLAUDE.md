# CLAUDE.md

このファイルは、本リポジトリで作業する Claude Code への指示書です。

## プロジェクト概要

トヨタ クラウン（セダン / HEV）のオーナーズマニュアル（以下 **OM**）の内容を、
視覚的にわかりやすく学べるようにしたウェブサイト。

対象オーナー層（クラウン購入層）に合わせ、**高貴で落ち着いたテイスト**を全体の基調とする。

### 収録コンテンツ

| 画面 | 対応する OM ページ | 内容 |
| --- | --- | --- |
| `cockpit.html` | OM P4 / P5 | 運転席まわりスイッチ（A〜R）＋ステアリングスイッチ |
| `climate.html` | OM P6 | エアコン（ヒーターコントロール）操作パネルのスイッチ |
| `meter.html` | OM P7 | コンビネーションメーターの警告灯 |

### 共通のインタラクション仕様

3画面とも同じ操作体系にすること。ここは本サイトの中核なので勝手に変更しない。

1. **ホバー** … 図中のスイッチ／警告灯にカーソルを合わせると、その部分が拡大表示される
   （ルーペ表示。ベクター拡大なのでぼやけない）
2. **クリック** … そのスイッチ／警告灯の説明文が詳細パネルに表示される
3. **図やリストの外をクリック** … ルーペと強調を解除する。
   読みかけの説明を消さないよう、詳細パネルの内容はそのまま残す（`Esc` は全解除）
4. 図と一覧リストは双方向に連動する（リスト側のホバー／クリックでも同じ挙動）
5. キーボード操作に対応する（`Tab` で移動、`Enter` / `Space` で決定、`Esc` で解除）

## 技術方針

- **素の HTML / CSS / JavaScript のみ**。ビルドツール・フレームワーク・パッケージ依存を持ち込まない。
  - 理由: ウェブ検索から直接たどり着いて、そのまま閲覧できる静的サイトであることを最優先するため。
- **外部 CDN に依存しない**。フォントは OS 標準の日本語明朝／ゴシックをフォントスタックで指定する。
- 図版は**インライン SVG**。以下の理由により画像ファイル（JPEG/PNG）は使わない。
  - 拡大してもぼやけない（ホバー拡大の要件を満たせる）
  - ホットスポットを図形座標で厳密に一致させられる
  - 第三者の著作物を同梱しなくて済む
- `<script>` は各 HTML の末尾で読み込む。モジュールバンドラは使わない。
- 対応ブラウザは各モダンブラウザの最新版。IE 対応は不要。

## ディレクトリ構成

```
.
├── index.html          トップ（3コンテンツへの導線）
├── cockpit.html        ① 運転席まわりスイッチ
├── climate.html        ② エアコン操作パネル
├── meter.html          ③ コンビメーター警告灯
├── assets/
│   ├── css/style.css   デザイントークン・レイアウト・コンポーネント
│   └── js/
│       ├── explorer.js       ホバー拡大／クリック説明の共通ロジック
│       ├── data-cockpit.js   ① のデータ
│       ├── data-climate.js   ② のデータ
│       └── data-meter.js     ③ のデータ
├── CLAUDE.md
└── README.md
```

### データの持ち方

各 `data-*.js` は `window.CROWN_DATA_<NAME>` にオブジェクトを代入する。
図（SVG）側のホットスポットは `data-id` 属性を持ち、データ側の `id` と一致させる。
**id が食い違うと動かない**ので、SVG を編集したらデータ側も必ず合わせること。

```js
{
  id: 'A',                    // SVG の data-id と一致させる
  label: 'インストルメントパネル照度調整スイッチ',
  badge: 'A',                 // 図中に表示する記号
  optional: true,             // ★ グレード・オプションにより装備の有無あり
  digitalOnly: true,          // ＊ 詳細はデジタル取扱説明書参照
  focus: [x, y, w, h],        // ルーペが拡大表示する SVG 座標領域
  desc: '…',                  // 説明文
  note: '…'                   // 補足（任意）
}
```

## 日本語の折り返し規約（最重要）

UI 上の日本語は、**文節を跨いで折り返さないこと**。とくに次の 2 つは不可。

- 語の途中で改行する（例：「クラウン」が「クラ／ウン」に割れる）
- 助詞を行頭に送る（例：「スイッチ／にカーソルを合わせる」）

実現方法は 3 つを併用する。

1. `assets/css/style.css` の `body` に `word-break: auto-phrase; line-break: strict;` を指定済み。
   対応ブラウザ（Chromium 系・Safari）はこれだけで文節折り返しになる。
2. **HTML に直接書く日本語は、文節ごとに `<span class="nw">` で包む。**
   `.nw` は `white-space: nowrap`。タグとタグの間に空白を作らないよう、改行はタグの中に入れる。

   ```html
   <p><span class="nw">気になるスイッチに</span
     ><span class="nw">カーソルを合わせれば</span
     ><span class="nw">その部分が拡大し、</span></p>
   ```

3. **JavaScript から流し込む文言は `jpEl()` / `jpText()`（`explorer.js`）を通す。**
   `textContent` に直接代入しない。語＋送りがな＋助詞をひとまとまりにして `.nw` で包む処理が入っている。
   `data-*.js` の `label` / `desc` / `note` / `source` はすべてこの経路を通る。

### 確認のしかた

文言を足したら、幅 375px と 700px の両方で、行頭が助詞や句読点になっていないか実際の描画で確かめる。
DevTools のコンソールで `Range#getBoundingClientRect()` を使うと行頭の文字を機械的に拾える。

## デザイン規約

- カラーは `assets/css/style.css` の `:root` にあるカスタムプロパティのみを使う。
  新しい色をその場で書かない。
- 見出しは明朝体、本文はゴシック体。見出しには字間（`letter-spacing`）を持たせる。
- 差し色は金（`--gold`）と紺（`--navy`）。彩度の高い色を広い面積に使わない。
- 余白を十分にとる。詰め込まない。
- 警告灯の色（赤／黄）は OM の表記に厳密に合わせる。**装飾目的で変更しない**。

## 内容の正確性について

- スイッチ名・警告灯名は OM および トヨタ公式「機能操作ガイド」の表記をそのまま使う。
  勝手に言い換えたり省略したりしない。
- 説明文の出典はトヨタ公式のデジタル取扱説明書。**推測で仕様を書かない**。
  裏が取れない項目は、断定を避けて公式ページへ誘導する。
- 装備の有無に関わる注記（★ / ＊）は OM の表記どおりに残す。
- 本サイトはトヨタ自動車株式会社の公式サイトではない。
  各ページのフッターにその旨と出典を必ず表示する。

## Git 運用ルール

**コードを変更するたびに GitHub へプッシュすること。** これが本プロジェクトの基本ルール。

- リモート: `https://github.com/wyattfuji1220/Crown-for-driver.git`
- 既定ブランチ: `main`

### 手順

1つの意味のあるまとまり（1ファイルの修正、1機能の追加など）ができたら、その都度：

```bash
git add -A
git commit -m "変更内容を日本語で簡潔に"
git push origin main
```

- **作業を溜めてからまとめてコミットしない。** 小さく、こまめに。
- コミットメッセージは日本語。何を変えたかが一行でわかるように書く。
- `push` まで完了して初めて「作業完了」とする。コミットだけで止めない。
- `--force` 系は使わない。履歴を壊さない。
- `--no-verify` でフックを飛ばさない。

### コミットメッセージの例

```
運転席まわりスイッチのホットスポットを追加
警告灯の色分けを OM の表記に合わせて修正
ルーペ拡大がタッチ操作で動かない不具合を修正
```

## 動作確認

ビルド不要。ファイルを直接開けば動く。

```bash
python -m http.server 8000
```

確認する項目：

- 3画面すべてでホバー拡大とクリック説明が動くこと
- 図とリストの連動が双方向で動くこと
- キーボードだけで全項目にたどり着けること
- 画面幅 375px（スマートフォン）でレイアウトが崩れないこと
